###################### Apps ######################

- Zoom
- Teams
- Slack
- Postman
- Chrome
- Brave Browser
- iTerm2
- IntelliJ
- VSCode
- Termius

#################### Dev Tools ###################

# dev
brew install git gh

# https://github.com/graalvm/homebrew-tap
brew install --cask graalvm/tap/graalvm-community-jdk22

# OpenJDK
brew install --cask temurin

brew install maven gradle
brew install sonarqube
brew install kafka

brew install --cask mysqlworkbench
brew install --cask nosqlbooster-for-mongodb
brew install mysql-client
brew install postgresql

brew install scala
brew install apache-spark
brew install apache-flink

brew install python
brew install node

# docker & k8s
brew install --cask podman-desktop
brew install kubernetes-cli
brew install helm
brew install fluxcd/tap/flux
brew install kubeseal
brew install k9s
brew install k3sup
brew install multipass
brew install cf

# ops
brew install ansible
brew install terraform
brew install awscli
brew install azure-cli

brew tap hashicorp/tap
brew install hashicorp/tap/vault

# security
brew install nikto nmap sqlmap sslscan
brew install --cask metasploit

###################### Utils #####################

# terminal
brew install --cask iterm2
brew install tmux
brew tap homebrew/cask-fonts && brew install --cask font-jetbrains-mono-nerd-font

brew install romkatv/powerlevel10k/powerlevel10k
brew install zsh-syntax-highlighting
brew install zsh-autosuggestions

=== iTerm2 settings ===
# Set zsh as Login shell
# Set tmux in Send text at start
# Set JetBrainsMono Nerd Font Mono - Regular - 12 as font
# Add Dracula.itermcolor file to Color Presets and set it as the theme

=== zsh, tmux, powerlevel10k settings ===
# Copy .zshrc, .tmux.conf, .tmux.conf.local, .p10k.zsh files to home directory

brew install vim nvim curl wget htop telnet jq ffmpeg
/usr/bin/python3 -m pip install --user pynvim

# tools
brew install --cask atom
brew install --cask termius
brew install --cask vlc
brew install --cask iglance
brew install yt-dlp
brew install mpv

brew install cmus
pip install cmus-osx
cmus-osx install
brew install --cask notunes

brew tap teamookla/speedtest
brew install speedtest

# funny
brew install sl
brew install cmatrix
brew install asciiquarium
